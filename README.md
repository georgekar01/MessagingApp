In this project i created a backend app that implements an event delivery system which supports real-time text and multimedia(photos and videos) messaging between clients. The project uses sockets and is developed using publisher-broker-consumer logic.
